/**
 * 曲线勾选控件的悬停说明：中文名、物理含义、公式（HTML title；多行显示依浏览器而定）。
 */
const BR = '\n'

export const CHART_METRIC_TOOLTIPS = {
  beta: `铺展系数 β${BR}物理含义：相对初始直径的无量纲铺展程度。${BR}公式：β = D / D₀（D 为当前铺展直径，D₀ 为标定初始直径）。`,

  thetaLeft: `左侧动态接触角 θ左${BR}物理含义：液–气界面在左侧三相接触线处与基底表面的夹角（侧视）。${BR}公式/方法：由接触线附近轮廓点回归得到界面取向，再换算为角度（°）。`,

  thetaRight: `右侧动态接触角 θ右${BR}物理含义：液–气界面在右侧三相接触线处与基底表面的夹角（侧视）。${BR}公式/方法：同 θ左，取右侧轮廓回归。`,

  thetaAvg: `平均接触角 θavr${BR}物理含义：左右接触角的算术平均，表征对称铺展时的代表接触角。${BR}公式：θavr = (θ左 + θ右) / 2。`,

  contactLineVelocity: `接触线速度 v${BR}物理含义：接触线沿基底铺展的速率（单侧）。${BR}公式：v = ½ · dD/dt（mm/s），D 为铺展直径，t 为相对时间。`,

  contactLineAccel: `接触线加速度 a${BR}物理含义：接触线速度的时间变化率。${BR}公式：a = dv/dt（mm/s²）。`,

  awa: `液–气界面积 A_wa${BR}物理含义：液滴自由（液–气）表面的总面积；轴对称下由子午母线绕对称轴旋转得到。${BR}公式：A_wa = ∮ 2π r ds（s 为母线弧长，r 到轴距离）。`,

  abase: `润湿基底面积 A_base${BR}物理含义：液滴与固体基底接触的圆盘（或触区）面积，用于 Young–Dupré 型能量项。${BR}公式：由接触宽度得直径 D 时，A_base ≈ π (D/2)²。`,

  deltaESigma: `表面能变化 ΔE_σ${BR}物理含义：相对触前理想球参考态的表面过剩能量（界面张力功）。${BR}公式：ΔE_σ = γ_wa A_wa + (γ_bw − γ_ba) A_base − π D₀² γ_wa。`,

  ek: `动能 E_k${BR}物理含义：铺展过程中液滴平动与「铺展」相关的动能近似。${BR}公式：E_k = ½ M [ V_cm² + ½ V_spread² ]，M = ρ_w π D₀³ / 6；V_cm、V_spread 由 Z_cm、D 对时间差分得到。`,

  emech: `机械能 E_mech${BR}物理含义：动能与表面能变化的标量和；实验数据中强制沿时间不增（禁止相对前一有效点的回升）。${BR}公式：E_k + ΔE_σ；曲线为单调护航后的值。耗散原始序列改用**同一帧未钳制的分量** E_k+ΔE_σ，以便与 E_total 求和闭合。`,

  wdiss: `耗散功 W_diss${BR}物理含义：相对参考时刻已耗散掉的机械能（累计）。${BR}**能量层保真**：max(0, E_mech(0)−(E_k+ΔE_σ))（分量），**不做**平滑或零点锚定，与 E_total 求和严格一致。E_mech(0) 优先首帧，否则首个有效帧。`,

  phi: `耗散功率 Φ${BR}物理含义：耗散功对时间的瞬时速率。${BR}**功率层降噪**：由**原始** W_diss 差分（内点中心差分，首尾单侧）得 raw Φ → 对 Φ 做 MA/SG（默认 7 点 MA）→ **max(0,·)** 展示。`,

  etotal: `总能量求和 E_total${BR}校验用：E_total = E_k + ΔE_σ + W_diss（原始 W_diss，无平滑）。${BR}与 E_mech(0) 偏差主要来自 max(0,·) 截断与浮点；Φ 的平滑不改变 W。开发模式下控制台可打印前 10 帧探针。`,

  volNorm: `体积 V 与归一化比 V/V₀${BR}纵轴为 V/V₀；悬停数据点可读出瞬时体积 V（mm³）。${BR}物理含义：V 为 Moore 外轮廓母线绕对称轴旋转得到的体积；V₀ = π D₀³ / 6 为参考球；V/V₀ 用于体积守恒 QC。${BR}公式：V₀ = π D₀³ / 6；V = π |∮ r² dz|；期望 V/V₀ ≈ 1。`,
} as const
